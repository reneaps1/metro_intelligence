# F4.3 — Servicio de auditoría + middleware de trazabilidad de cambios

**Notion:** MI-23 — https://app.notion.com/p/3984c8662e5781a18e24ce2ca26e8e1c
**Fase:** F4 · Backend MVP | **Scope:** BOTH | **Prioridad:** P1 Alta | **Estimación:** S (½ día)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## Estado de dependencias

- **F2.1:** ✅ Done (`security_audit_log` table + append-only trigger already exist).
- **F4.2 (auth):** ❌ Bloqueador — necesitas el contexto de usuario autenticado (actor) que F4.2 provee. Verifica que exista.

## 🎯 Objetivo

Servicio central de auditoría: un helper que registra en `security_audit_log` (actor, acción, entidad, before/after JSONB, ip, timestamp), y el patrón de uso obligatorio para todo servicio que escriba master data, decisiones, o usuarios.

## 📋 Contexto

CLAUDE.md §5/§23: auditoría requerida para logins, cambios de permisos, cambios de master data, y cada recomendación/decisión. **Debe ser imposible de olvidar** — se integra como decorador o llamada explícita, verificada en code review + test.

## ✅ Alcance

- `app/services/audit_service.py` con API tipada (algo como `audit_service.record(actor, action, entity_type, entity_id, before, after, ip)`).
- Captura de before/after para updates de master data.
- Contexto de request (user, ip) vía FastAPI dependency.
- Documenta el patrón de uso en `backend/README.md`.

## 🚫 Fuera de alcance

Logs inmutables con hash-chain (fase futura); UI de auditoría (post-demo).

## 📁 Archivos esperados

`backend/app/services/audit_service.py` + tests.

## Referencias obligatorias

- `backend/app/models/security.py` — modelo `AuditLog`.
- `backend/alembic/versions/0001_org_security.py` — el trigger append-only sobre `security_audit_log` (bloquea UPDATE/DELETE incluso para el servicio; tu código solo debe hacer INSERT).

## ✔️ Criterios de aceptación

- [ ] Un update de una spec registra before/after correctos (test).
- [ ] La entrada de auditoría incluye el actor real del request (no un placeholder).
- [ ] Sin PII innecesaria en las entradas.

## ⚠️ Riesgos

Olvidar auditar en algún servicio nuevo — el checklist §18.4 de CLAUDE.md debe correrse en cada PR futuro que toque escrituras.

## 🧪 Testing

Un test por tipo de operación auditada.

## Al terminar

Ver `docs/tasks/README.md`.
