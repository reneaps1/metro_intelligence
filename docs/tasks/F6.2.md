# F6.2 — Dashboard operativo

**Notion:** MI-40 — https://app.notion.com/p/3984c8662e5781dd84c2f73cac278927
**Fase:** F6 · Dashboards y reportes | **Scope:** DEMO | **Prioridad:** P0 Crítica | **Estimación:** M (1-2 días)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## Estado de dependencias

- **F6.1 (charts):** en gran parte ya resuelto por el mock — ver `docs/tasks/F6.1.md`.
- **F4.6 (mediciones), F4.8 (recomendaciones):** ❌ Bloqueadores directos.

## ⚠️ Importante: ya existe una versión completa contra datos mock

`frontend/src/features/dashboards/OperationalDashboard.tsx` (F5.M) ya implementa: grid de salud por parte (StatusChip "All OK" / "N NOK"), feed de últimos runs, y feed de "alertas activas" (recomendaciones pendientes) con link a la característica correspondiente.

**Esta tarea reemplaza `useDemoData()` por llamadas reales**, y evalúa si hace falta un endpoint agregador en el backend (coordinar con quien hizo F4.6) para evitar N+1 requests — el mock actual arma todo en el cliente a partir de los arrays completos de partes/características, lo cual no escala contra una API real paginada.

**Lo que falta añadir:**

- Respeto a "tarjetas fijadas del usuario" (preferencias) — no existe en el mock (es una feature de personalización fuera del alcance original de F5.M).
- Estados empty/loading/error explícitos por widget — el mock asume que los datos siempre están disponibles (nunca hay loading real porque los datos son síncronos).

## Referencias obligatorias

- `frontend/src/features/dashboards/OperationalDashboard.tsx` — punto de partida.
- El contrato de F4.6/F4.8.

## 🚫 Fuera de alcance

Vista ejecutiva (F6.3).

## ✔️ Criterios de aceptación

- [ ] Carga completa en menos de 2s sobre el seed.
- [ ] Todo elemento clicable lleva al detalle correspondiente (ya cumplido en el mock).
- [ ] Estados empty/loading/error en cada widget (nuevo).
- [ ] Ambos temas verificados.

## Al terminar

Ver `docs/tasks/README.md`. Golden path, paso 8.
