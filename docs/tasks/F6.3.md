# F6.3 — Dashboard ejecutivo

**Notion:** MI-41 — https://app.notion.com/p/3984c8662e5781f9ac93d749ae798d4c
**Fase:** F6 · Dashboards y reportes | **Scope:** DEMO | **Prioridad:** P1 Alta | **Estimación:** M (1-2 días)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## Estado de dependencias

- **F6.1 (charts):** en gran parte ya resuelto — ver `docs/tasks/F6.1.md`.
- **F4.6, F4.8:** ❌ Bloqueadores directos. Enriquecido más adelante por F8.D–F10.D si ya existen.

## ⚠️ Importante: ya existe una versión contra datos mock

`frontend/src/features/dashboards/ExecutiveDashboard.tsx` (F5.M) ya implementa: StatTiles con Cpk promedio, tasa OK con sparkline, "oportunidades de ahorro de inspección" (contando recomendaciones `frequency_decrease`), recomendaciones pendientes, y un gráfico de barras simple de tendencia semanal de OK-rate.

**Esta tarea reemplaza los cálculos hechos en el cliente (`frontend/src/lib/mock/analytics.ts`) por datos que vienen ya calculados del backend**, y construye lo que falta:

- El widget central del pitch comercial — **"estrategia de inspección: frecuencia actual vs recomendada, horas estimadas ahorradas, riesgo asociado"** — no existe todavía en el mock en esa forma explícita (el mock solo cuenta cuántas recomendaciones de tipo `frequency_decrease` hay, no calcula horas ahorradas ni muestra el riesgo asociado junto al ahorro).
- Filtros por periodo/familia/línea — no existen en el mock.
- Heatmap de riesgo por familia × semana usando `RiskHeatmap` de F6.1 (el mock usa el heatmap pero por característica individual, no agregado por familia).

## 📋 Contexto

**El widget de ahorro es el argumento de venta central** ("optimizar estrategia de inspección, reducir sin aumentar riesgo" — CLAUDE.md §1). **Nunca muestres el ahorro sin el riesgo asociado al lado** — esto es un requisito de diseño explícito, no solo una sugerencia.

## Referencias obligatorias

- `frontend/src/features/dashboards/ExecutiveDashboard.tsx`, `frontend/src/lib/mock/analytics.ts` (los cálculos de Cpk/OK-rate que probablemente se muevan al backend).
- El contrato de F4.6/F4.8 y, si ya existen, F8.D/F9.D/F10.D.

## 🚫 Fuera de alcance

Exportación a PDF; cálculo real de frecuencias óptimas (eso es F10.D, aquí solo se muestra lo que ese motor produzca).

## ✔️ Criterios de aceptación

- [ ] Los KPIs cuadran con un cálculo manual sobre el seed (validación cruzada explícita, no solo "se ve razonable").
- [ ] El widget de ahorro siempre comunica el riesgo junto al beneficio.
- [ ] Ambos temas verificados.

## Al terminar

Ver `docs/tasks/README.md`. Golden path, paso 9 — la pantalla que cierra la venta.
