# F4.1 — Esqueleto FastAPI: config, logging estructurado, health, estructura de módulos

**Notion:** MI-21 — https://app.notion.com/p/3984c8662e578120b892cf7fd85bab57
**Fase:** F4 · Backend MVP | **Scope:** BOTH | **Prioridad:** P0 Crítica | **Estimación:** M (1-2 días)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## Estado de dependencias

- **F0.1 (arquitectura base):** ✅ Done.
- Coordina con F1.2 (CI de backend) — si F1.2 aún no existe cuando hagas esto, no es un bloqueador, pero deja tu `pyproject.toml` listo para que F1.2 lo use.

Este es el primer bloqueador real de todo F4 — F4.2 a F4.9 no pueden empezar hasta que esto exista. Es la tarea más urgente de las 24.

## 🎯 Objetivo

Crear el esqueleto del monolito modular FastAPI según `backend/README.md`: config por env (pydantic-settings), logging JSON estructurado, endpoint `/health`, y la estructura `api/core/models/schemas/repositories/services/engines/connectors`.

## 📋 Contexto

Ver `docs/adr/001-modular-monolith.md` y `docs/adr/002-python-fastapi-single-stack.md`. Todo módulo posterior se cuelga de esta estructura; los agentes en paralelo dependen de que las fronteras queden claras desde el inicio.

**Ya existe parcialmente:** `backend/app/main.py` (un placeholder mínimo creado en F1.4 con un solo `GET /health` — ver su contenido actual, está marcado explícitamente como placeholder para esta tarea) y `backend/requirements.txt` (dependencias mínimas para migraciones, no para correr una app FastAPI real — probablemente necesites ampliarlo). `backend/Dockerfile` ya existe (F1.4) y espera que `app.main:app` sea una app FastAPI real corriendo con uvicorn — no rompas ese contrato.

## ✅ Alcance

- `pyproject.toml` con dependencias pinneadas, config de ruff/mypy strict (reemplaza o complementa `backend/requirements.txt` — tu decisión, pero deja claro cuál es la fuente de verdad).
- `app/core/config.py`: settings desde env (pydantic-settings), validando contra `.env.example`.
- Logging JSON estructurado con `request_id`; middleware de errores que **nunca** filtra stack traces (respuestas de error saneadas — CLAUDE.md §18.5).
- `/health` (único endpoint sin auth) y router base `/api/v1`.
- Estructura de directorios completa con `__init__.py` y docstrings de responsabilidad, tal como la describe `backend/README.md`.

## 🚫 Fuera de alcance

Auth (F4.2), modelos de dominio (ya existen, los definió F2.x — no los toques aquí).

## Referencias obligatorias

- `backend/README.md` — el layout objetivo exacto.
- `docs/adr/001-modular-monolith.md`, `docs/adr/002-python-fastapi-single-stack.md`.
- `.env.example` — toda config nueva que necesites debe documentarse aquí con placeholder.
- `backend/app/main.py`, `backend/Dockerfile`, `backend/requirements.txt` — ya existen, revísalos antes de escribir nada.

## ✔️ Criterios de aceptación

- [ ] `uvicorn app.main:app` levanta; `/health` responde `{"status": "ok"}`.
- [ ] Un error interno produce una respuesta genérica sin internals (test que fuerce una excepción y verifique el body de la respuesta).
- [ ] `ruff` y `mypy --strict` pasan.
- [ ] Toda config viene de env; cero valores hardcodeados.

## ⚠️ Riesgos

Una estructura débil aquí se propaga a todo lo demás — sigue `backend/README.md` al pie de la letra, no inventes una organización distinta.

## 🧪 Testing

Test de `/health`, de manejo de errores, y de carga de config (falla claramente si falta una env var requerida).

## Al terminar

Ver `docs/tasks/README.md`. Esta tarea desbloquea F4.2–F4.9 — avisa explícitamente en Notion que quedaron desbloqueadas al terminar.
