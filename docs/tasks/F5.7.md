# F5.7 — Pantallas de mediciones

**Notion:** MI-36 — https://app.notion.com/p/3984c8662e5781e6821ec40b47250dc5
**Fase:** F5 · Frontend MVP | **Scope:** BOTH | **Prioridad:** P1 Alta | **Estimación:** L (3-5 días)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## Estado de dependencias

- **F4.6 (API de mediciones):** ❌ Bloqueador directo.
- **F6.1 (componentes de charts):** coordina — ver más abajo, probablemente ya está resuelto por el mock.

## ⚠️ Importante: la UI y los gráficos ya existen

`frontend/src/features/measurements/MeasurementsListPage.tsx` y `CharacteristicTrendPage.tsx` (F5.M) ya implementan: listado de runs con `StatusChip` por conteo de NOK, listado de todas las características con Cpk calculado en el cliente, y una vista de característica con **trend chart I-MR completo** (`frontend/src/components/charts/TrendChart.tsx` — nominal/límites como líneas de referencia, violaciones NOK marcadas con triángulo, no solo color) + histograma de capacidad (`Histogram.tsx`).

**Esta tarea es sobre todo: reemplazar `useDemoData().getSeries()` por llamadas reales a `GET /characteristics/{id}/series` (F4.6)**, y añadir lo que falta:

- Registro manual de eventos de proceso desde esta vista (form modal) — no existe en el mock.
- Marcadores de eventos de proceso superpuestos en la serie — el mock muestra un aviso de texto sobre el evento (`CharacteristicTrendPage.tsx`), no un marcador visual en el propio gráfico.
- Zoom por rango de fechas — no existe en el mock.
- Downsampling real vía `max_points` del API (el mock no lo necesita porque son pocos puntos).

## 📋 Contexto

Los valores siempre en monospace con decimales fijos por unidad (ya cumplido en el mock).

## Referencias obligatorias

- `frontend/src/features/measurements/*.tsx`, `frontend/src/components/charts/TrendChart.tsx`, `Histogram.tsx` — punto de partida real, ya visualmente pulido y verificado con Playwright.
- El contrato de F4.6 y F4.7 (eventos de proceso — si F4.7 no está entre estas 24 tareas es porque no apareció en la lista de pendientes; verifica su estado en Notion antes de asumir que existe).

## 🚫 Fuera de alcance

Control charts SPC completos (eso lo integraría una fase posterior); dashboards (F6.x).

## ✔️ Criterios de aceptación

- [ ] La serie de 90 días se ve fluida (usa downsampling si hace falta).
- [ ] Los NOK están marcados con color+forma, no solo color (ya cumplido en el mock — verifica que se mantenga).
- [ ] Un evento creado desde la UI aparece superpuesto en la serie.
- [ ] Ambos temas verificados.

## Al terminar

Ver `docs/tasks/README.md`. Golden path, pasos 4-5.
