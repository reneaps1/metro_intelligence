# F5.5 — Pantallas de catálogo maestro

**Notion:** MI-34 — https://app.notion.com/p/3984c8662e578112a598fcada67a41f7
**Fase:** F5 · Frontend MVP | **Scope:** BOTH | **Prioridad:** P1 Alta | **Estimación:** L (3-5 días)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## Estado de dependencias

- **F4.4 (API de catálogo):** ❌ Bloqueador directo.
- **F5.4 (sesión real):** recomendado tenerlo primero, aunque técnicamente podrías avanzar la UI antes.

## ⚠️ Importante: la UI de lectura ya existe

`frontend/src/features/catalog/CatalogListPage.tsx` y `PartDetailPage.tsx` (F5.M) ya muestran: listado de partes, detalle de parte con tabla de características (balloon, tipo, clasificación con `StatusChip`, spec vigente en texto formateado, último resultado OK/NOK), todo contra datos mock (`useDemoData()` de `frontend/src/lib/mock/DataProvider.tsx`).

**Esta tarea es, en gran parte: reemplazar `useDemoData()` por llamadas reales a la API de F4.4**, y **construir lo que falta**:

- Búsqueda/filtros en el listado (el mock no los tiene).
- Formularios de alta/edición de specs (el mock es 100% read-only — no hay ningún formulario de escritura en el catálogo todavía).
- Timeline de versiones de spec (el mock genera una característica con historial de versiones en los datos de F3.2, pero la UI actual solo muestra la spec vigente, no el historial completo).

## 📋 Contexto

Al editar una tolerancia, el formulario debe dejar claro que se crea una **versión nueva** con vigencia — nunca una sobrescritura. Permisos: `admin`/`quality_engineer` editan, el resto solo lee.

## Referencias obligatorias

- `frontend/src/features/catalog/CatalogListPage.tsx`, `PartDetailPage.tsx`, `frontend/src/lib/format.ts` (helpers de formato de specs ya existentes) — punto de partida.
- El contrato de F4.4.

## 🚫 Fuera de alcance

Importación masiva de catálogo; visualización de mediciones (eso es F5.7).

## ✔️ Criterios de aceptación

- [ ] CRUD completo funcional contra el backend con el dataset seed.
- [ ] Editar una tolerancia muestra y crea una nueva versión, verificable en un timeline.
- [ ] `viewer` no ve botones de edición.
- [ ] Ambos temas verificados.

## Al terminar

Ver `docs/tasks/README.md`. Parte del golden path del demo.
