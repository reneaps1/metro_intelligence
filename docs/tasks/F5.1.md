# F5.1 — Setup Vite + Tailwind + tokens del design system

**Notion:** MI-30 — https://app.notion.com/p/3984c8662e57812da0a4db6f14c82c43
**Fase:** F5 · Frontend MVP | **Scope:** BOTH | **Prioridad:** P0 Crítica | **Estimación:** M (1-2 días)

> Read `CLAUDE.md` at the repo root fully before starting. Read `docs/tasks/README.md` for the shared workflow this file assumes.

## ⚠️ Importante: gran parte de esto ya existe

`frontend/` ya tiene un scaffold **Vite + React 18 + TypeScript + Tailwind funcionando**, construido durante F5.M (el demo mock) y verificado con Playwright en ambos temas. Antes de escribir una sola línea, revisa lo que ya está:

- `frontend/package.json`, `vite.config.ts`, `tailwind.config.ts`, `tsconfig*.json` — ya existen.
- `frontend/src/theme/tokens.css` — ya tiene **todos** los tokens de `docs/design/design-system.md` §1 como variables CSS, light y dark, incluyendo OK/NOK/warning/info/neutral y la paleta de charts.
- `frontend/src/index.css` — importa las fuentes self-hosted (`@fontsource/inter`, `@fontsource/jetbrains-mono`), aplica Tailwind, incluye `prefers-reduced-motion`.
- `frontend/src/lib/theme/ThemeProvider.tsx` — toggle de tema funcional, persistido en localStorage, respeta `prefers-color-scheme` como default.
- Lucide ya está instalado y en uso.

**Esta tarea, entonces, es sobre todo cerrar lo que le falta a ese scaffold para cumplir los criterios de aceptación reales**, no reconstruirlo:

## ✅ Lo que probablemente SÍ falta

- ESLint config (regla que impida `any` sin comentario de justificación, regla custom o stylelint que rechace hex crudos fuera de `tokens.css`).
- Prettier config.
- Vitest config (el scaffold actual no tiene tests unitarios de componentes, solo se verificó con Playwright manualmente).
- Verificación formal de contraste AA en ambos temas (documentar cómo se verificó, no solo asumir).
- Verificar cero requests a orígenes externos (network tab / CSP) — probablemente ya cumple (todo self-hosted) pero no se verificó explícitamente con ese criterio en mente.

## 📋 Contexto

`docs/design/design-system.md` es el contrato — ningún componente puede usar hex crudos. On-premise: cero CDNs, fuentes e iconos empaquetados (ya cumplido).

## 🚫 Fuera de alcance

Componentes UI nuevos (eso sería F5.2 — Storybook, fuera del alcance de las 24 tareas core); layouts (F5.3, que también ya tiene una versión mock — ver `docs/tasks/F5.3.md`).

## Referencias obligatorias

- `docs/design/design-system.md` — el manual completo.
- `frontend/src/theme/tokens.css`, `tailwind.config.ts` — lo que ya existe, verifica que coincida exactamente con el manual antes de dar la tarea por cumplida (podría haber pequeños desvíos del mock original).

## ✔️ Criterios de aceptación

- [ ] La app arranca en ambos temas sin FOUC; el toggle es instantáneo (ya cumplido, verificar).
- [ ] Cero requests a orígenes externos (verificar explícitamente).
- [ ] Contraste AA verificado para los pares texto/fondo en ambos temas (documentar el método de verificación).
- [ ] El lint falla si un componente usa un hex crudo (esto probablemente NO existe todavía — es la pieza más real de trabajo nuevo en esta tarea).

## Al terminar

Ver `docs/tasks/README.md`. Si terminas y realmente no queda nada más que verificar/documentar, dilo así de claro en el PR — no inventes trabajo que no hace falta.
