"""Orchestration / use cases: coordinates repositories/ and engines/ to
fulfill one API operation (e.g. "import a file", "accept a recommendation").
Routers call services, never repositories or engines directly."""
